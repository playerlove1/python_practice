AdalineGD  for(迭代回合數)
                predict=所有樣本的預測值矩陣   (每個預測值為   特徵向量跟權重 內積(即 w1x1+w2x2+w3x3+...))
                errors=所有樣本的誤差矩陣  (y-predict)
                更新權重 = 學習速率* (所有樣本的  特徵向量矩陣.T 跟 誤差 的內積 (即 根據所有樣本 統合出該權重要往哪邊修正))
                計算每回合Cost值
            
           
AdalineSGD  for(迭代回合數)
                for(所有樣本)
                    predict=樣本的預測值矩陣(預測值為   特徵向量跟權重 內積(即 w1x1+w2x2+w3x3+...))
                    errors=樣本的誤差  (y-predict)
                    更新權重 = 學習速率* (樣本的  特徵向量矩陣.T 跟 誤差 的內積 (即 目前樣本 看該權重要往哪邊修正))
                    計算每個樣本的Cost值
                計算每回合Cost值=(經歷過所有樣本之平均Cost值) 